(()=>{try{const before=cameraLocations.length;cameraLocations=cameraLocations.filter(cam=>{const id=String(cam.id||cam.node_id||'');const isBuiltInDemo=id.startsWith('ALPR-')&&!cam._cloud&&!cam._manual;const text=`${cam.label||''} ${cam.type||''} ${cam.source||''}`.toLowerCase();const isPriority=/flock|alpr|license\s*plate|plate\s*reader|automatic\s*license|\blpr\b/.test(text);const isTrafficEnforcement=!isPriority&&/red[- ]?light camera|speed camera|traffic enforcement camera/.test(text);return !isBuiltInDemo&&!isTrafficEnforcement});const removed=before-cameraLocations.length;if(typeof renderCameras==='function')renderCameras();if(removed>0&&typeof showHudToast==='function')showHudToast(`${removed} non-target/demo nodes removed • showing surveillance-network nodes only`);console.info(`GhostLane production node filter removed ${removed} non-target/demo nodes.`)}catch(e){console.error('GhostLane production node filter failed',e)}})();